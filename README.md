# PrasTestProject
Проект для демонстрации архитектурных решений на ASP.NET Core с использованием PostgreSQL, MediatR, AutoMapper и контейнеризации через Docker Compose.

## 📦 Требования
- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/)

Убедитесь, что установлены последние версии.

## 🧩 Технологии проекта
В проекте используются следующие технологии и инструменты:

- **Docker & Docker Compose**  
  Для контейнеризации и оркестрации сервисов. Обеспечивает воспроизводимость окружения и простоту запуска.

- **ASP.NET Core MVC**
  - Бэкенд реализован на C#, с акцентом на чистую архитектуру, строгую типизацию и модульность. 
  - Чистая архитектура: бизнес-логика вынесена в сервисы.  
  - ViewModels используются только для UI-слоя.
  
- **PostgreSQL**  
  Реляционная база данных для хранения основной бизнес-информации.  
  Особенности:
  - Надёжность и транзакционная целостность (ACID)
  - Поддержка сложных запросов и индексов
  - Используется как основной источник данных для backend API
  - Данные хранятся в отдельном volume, что обеспечивает сохранность при перезапуске контейнеров

- **MediatR**  
  - Реализация паттерна CQRS.  
  - Чёткое разделение команд и запросов.  

- **AutoMapper**  
  - Автоматический маппинг DTO ↔ Domain моделей.  
  - Минимизация boilerplate-кода.
  - 
## 🔑 Ключевые принципы
- **Чистая архитектура**: разделение слоёв и строгие границы между инфраструктурой и бизнес-логикой.  
- **Повторное использование**: модульные Razor partial views и TagHelpers.  
- **Локализация**: поддержка русского и английского интерфейсов через ресурсные файлы.  
- **Устойчивость**: централизованная обработка ошибок, транзакционная целостность (DB + файловая система).  
- **Масштабируемость**: модульная структура, лёгкое расширение.

  ## 🚀 Запуск проекта
1. Клонируйте репозиторий:
   git clone https://github.com/romanshal/PrasTestProject.git
   cd PrasTestProject
2. Запустите сервисы:
   docker-compose up -d
